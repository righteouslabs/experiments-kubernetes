server:
  port: 8080

spring:
  application:
    name: document-consumer

app:
  supported-versions: ${SUPPORTED_VERSIONS:1,2,3}
  dapr:
    pubsub-name: kafka-pubsub
    topic: documents
    statestore-name: mongodb-statestore
    http-port: ${DAPR_HTTP_PORT:3500}
