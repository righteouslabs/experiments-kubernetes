##
## Pipeline Dashboard — lightweight FastHTML web UI
##
## Shows real-time consumer stats, Schema Registry subjects,
## and Dapr routing rules. Auto-refreshes every 3 seconds.
##
## Access via: kubectl -n versioned-demo port-forward svc/dashboard 5001:5001
## Then open: http://localhost:5001
##
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
  namespace: versioned-demo
  labels:
    app: dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      enableServiceLinks: false
      containers:
        - name: dashboard
          image: docker.io/library/demo-dashboard:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 5001
          env:
            - name: CONSUMER_V1_URL
              value: "http://consumer-v1.versioned-demo.svc.cluster.local:8080"
            - name: CONSUMER_V2_URL
              value: "http://consumer-v2.versioned-demo.svc.cluster.local:8080"
            - name: CONSUMER_V3_URL
              value: "http://consumer-v3.versioned-demo.svc.cluster.local:8080"
            - name: SCHEMA_REGISTRY_URL
              value: "http://schema-registry.versioned-demo.svc.cluster.local:8081"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: versioned-demo
spec:
  selector:
    app: dashboard
  ports:
    - port: 5001
      targetPort: 5001
---
## Services for consumers so the dashboard can reach them by name
apiVersion: v1
kind: Service
metadata:
  name: consumer-v1
  namespace: versioned-demo
spec:
  selector:
    app: consumer
    version: v1
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: consumer-v2
  namespace: versioned-demo
spec:
  selector:
    app: consumer
    version: v2
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: consumer-v3
  namespace: versioned-demo
spec:
  selector:
    app: consumer
    version: v3
  ports:
    - port: 8080
      targetPort: 8080
