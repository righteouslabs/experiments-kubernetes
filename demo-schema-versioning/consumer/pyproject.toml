[project]
name = "demo-consumer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "pydantic>=2.7",
  "uvicorn[standard]>=0.30",
]

[tool.ruff]
line-length = 110
target-version = "py312"
